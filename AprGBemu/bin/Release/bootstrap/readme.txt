這個目錄放置GameBoy Color與GameBoy所使用的bootstrap檔,

GameBoy Color : 請將名稱為 gbc_bios.bin 檔案放於此處
GameBoy : 請將名稱為 DMG_ROM.bin 檔案放於此處

若檔案下載的檔名有差異,可以自行更名所下載的檔案名稱(否則程式無法偵測到).

如果此目錄有放置正確的遊戲主機bootstrap檔,

模擬器的設定選項將可以選擇使用bootstrap過程開機

(會跑出Nintendo的logo入場動畫).

受限於版權法律規範,不直接提供檔案.